
#include <debug.h>

#include "struct.h"
#include "new.h"

struct tokensetset* new_tokensetset()
{
	ENTER;
	TODO;
	#if 0
	struct tokensetset* this = smalloc(sizeof(*this));
	
	this->data = NULL;
	this->cap = 0;
	this->n = 0;
	
	EXIT;
	return this;
	#endif
}

